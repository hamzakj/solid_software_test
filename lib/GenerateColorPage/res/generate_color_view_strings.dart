///string values in Generate Color page module
class GenerateColorViewStrings {
  ///app bar title in Generate Color page view
  static const appBarTitle = 'Generate Color Page';

  ///center word in Generate Color page view
  static const centerWord = 'Hello there';
}
